# Roadmap

## 0.5
- `spacecrab [PATH]`, `--help`, `--version`
- Exit codes: 0 ok, 1 incomplete, 2 usage error

## 0.6
- Show folder totals, sorted by size
- Size on disk by default, hard links counted once
- `-b` for exact bytes
- Numbers checked against `du` in CI

## 0.7
- `-x` to stay on one filesystem

## 0.8
- Document the output format and the core API

## 1.0
- Use 0.8 daily for two weeks, then tag
